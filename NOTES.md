这一版修的是两处「用起来应该有的东西没有」：长条勾了却不显示、统计只有总数没法分段看。

## 一、长条：勾了多少就显示多少

以前在「长条显示内容」里勾了七八项，长条上永远只有四项。

根子是长条宽度上限被一个 `560` 的常量卡成了真上限，而「宽度不够就从后往前丢字段」
那段逻辑**看起来完全正常** —— 它不是坏了，是老老实实执行了一个错的前提。
本机任务栏 2560 宽、图标簇居中，开始按钮左边其实有 **863px** 空地，
一个字段约 100px，单行物理上就只放得下 5~6 项。

放开上限只解决一半，所以这一版把「装不下」这件事做成了四层递进，
**每一层都只在需要时才启用**，字段少的时候外观和以前逐像素一致：

1. **折行**：一行放不下就加第二行（最多三行），行高按字号自动算；
2. **紧凑标签**：「本次电费」→「本次」、「处理器」→「CPU」，密的时候才换；
3. **收紧间距**：分段之间的分隔和间隙按 40% / 70% 压；
4. **再降一档字号**：新增「极小」档，两行还是塞不下时用。

现在实测：勾 4 项是单行原字号（和以前一样），勾 8 项两行，**勾满全部 15 项也全排上**，
两行 47px 高、稳稳贴在任务栏里。可用宽度真的不够时（比如同时开了一大堆程序
把开始按钮挤到很左边），才按顺序从后往前少显示几项 —— 少显示也比整条消失好。

顺带修了两个折行过程中暴露出来的真问题：

- **排版计划不能再跟着读数走**。一开始按当前数值算「排几行 / 用哪档字号」，
  结果 6 个字段时正好卡在边界：空闲 9 W 是单行、跑起来 132 W 就要折行，
  字号在 1.00 和 0.85 之间来回弹、长条高度跟着跳。现在规划时把数值换成固定的
  占位符（「999」「¥99.99」），计划只由「勾了哪些字段」决定，读数怎么变都不动。
- **单行时画布必须占满胶囊高度**。折行改造时画布高度被写成「行高 × 行数」
  （单行只有 32.5px，而窗口是 47px 高的胶囊），于是底色和描边只画了上面一截，
  下面那条从没被填过 —— 叠上半透明就是一条黑带。

## 二、用量统计：能按时间分段看了

以前只有一个「累计」，想知道「这个月用了多少」「每次开机各用了多少」都看不到。
托盘右键新增 **「用量统计…」**，开一个正经的窗口（原生 Tab + 表格，
支持滚动 / 列宽拖拽 / 滚轮）：

| 分页 | 内容 |
| --- | --- |
| 按天 | 每天一行：电量 / 电费 / 运行时长 / 当天开机几次 |
| 按月 | 各月合计 + 天数 + 日均 |
| 按年 | 各年合计 + 天数 + 日均 |
| **按每次开机** | **每一次**开机单独一条（不是「当前这次」）：开机时刻 / 电量 / 电费 / 运行时长 / 这次一共开了多久 |
| 按时段 | 0~23 点各累计多少，找「几点最费电」，带占比 |

- 「每次开机」的键是**开机时刻**：同一次开机内关掉程序再打开，接在同一条上；
  重启之后才是新的一条。账本保留最近 **240 次开机**、**400 天**。
- 底部一直显示今天 / 本月 / 今年 / 累计四个总数，宽度不够时按重要性丢
  （先丢「今年」，累计一定留着）。
- 表格里的数字全部来自实际运行期间的功率积分，程序没跑的那段测不到 —— 窗口底部
  写了这句话，免得对着偏小的数字纳闷。

## 三、自检

`能耗统计.exe --self-test` 从 49 项扩到 **57 项**。新增的检查里有几条是专门
守着`「只有点开界面才看得出来」的那类故障**：

- 长条：单行画布是否占满胶囊高度（防黑带）、勾满所有字段是否一个不丢、字段多时是否真的折行；
- 统计窗口：**真的把窗口建出来、把表格格子用 `LVM_GETITEMTEXTW` 读回来核对**。
  这类 bug 单元测试抓不到 —— 原生控件的文字是靠指针传进去的，传错时控件显示乱码
  而「行数 / 列数 / 选中下标」全是好的；`_lint.py` 也加了条静态检查，
  禁止把窗口样式位传进 `CreateWindowExW` 的扩展样式参数（那个位置上的
  `WS_CLIPCHILDREN` 恰好等于 `WS_EX_COMPOSITED`，会让表格表体一个字都不画）。
