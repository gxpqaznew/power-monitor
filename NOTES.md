这一版接着修「右键弹不出菜单」—— **上一版的修法还留了一条缝**，顺便把「长条外观」
里一个会静默失效的坑补上。

## 一、重新注册那几秒，托盘是卡住的（真 bug，用户报的那个的**第二半**）

上一版（v1.0.15）修的是「注册信息被越改越窄 → 回调根本没登记进去 → 右键从此永久失灵」。
这一版修的是同一条路上的另一半：

**「重新注册」这件事是在窗口过程里同步做的，而它的失败重试会「睡一会儿再试」。**
平时没事（图标还在，重复注册会被 Shell 拒绝，但那不是故障），可一旦走到这条路上，
程序就会在自己的消息循环里连着睡 **4 秒**。这 4 秒里托盘窗口收不到任何鼠标消息 ——
右键点了没反应、双击也没反应，几秒之后又自己好了。用户看到的就是
「刚点了『常驻任务栏』，右键怎么按都弹不出菜单」。

实测数据（可复现）：

| | 老版本 | 现在 |
|---|---|---|
| 从收到「重新注册」到右键弹出菜单 | **5.55 s** | **< 1 s** |
| 单次重新注册的阻塞时间 | 4.00 s | 0 s |

修法两条一起上：

* **不许阻塞** —— 重新注册只试一次，不等；不行就交给每秒的定时器慢慢补。
* **图标还在就别硬注册** —— 实测「对已经存在的图标重复注册」会被 Shell **立刻**拒绝
  （E_FAIL，0.6 毫秒），但紧接着的「修改」调用是**成功**的。也就是说图标本来就是好的，
  把回调信息重新登记一遍就够了，根本不用等。顺带还修掉一个隐患：以前这里失败会被
  误判成「系统没有通知区域」，进而把程序降级成普通窗口（用户连入口都没了）。

## 二、背景图不透明度写成小数会静默不生效（真 bug）

「不透明度」的单位是 **0~100**。这个字段在配置文件里是明文，手改的时候很自然会写成
`0.85` 这种小数 —— 而 `0.85` 取整就是 **0**，0 的语义是「不画背景图」。

于是表现成：设了背景图、滑块也拉上去了、图片也明明在那儿，**就是什么都不显示**，
而且一声不响（图片读不到、路径不对、解码失败，全都是安静跳过）。

现在小数按分数认：`0.85` → 85%、`1.0` → 100%。整数照旧 —— 写 `1` 就是 1%。
（界面上的滑块一直是百分比，不受影响。）

## 三、回归

* 自检 90 项；`_appearancetest.py` 49 → **50**（补了上面第二条）；
* 新增 `_appearancelive.py`（**22 项**）：真机真显示、真 `SendInput` 点击、从**屏幕**上
  抓图 —— 离屏测试用的是合成消息，验不了「显示出来之后长什么样、真鼠标点不点得动」。
  里面用一张「洋红 → 青」的渐变图当背景探针：真铺上去了两种颜色各会出现几千个像素，
  不铺就是 0（判据按实测背景噪声 170 px 的 3 倍起算，够不着就判 FAIL）。
* `_traylive.py` 改成**量时间**而不是问「最终能不能弹出来」：老版本问法在真机上
  7/8 全绿，把 4 秒阻塞放过去了；改成量墙钟之后立刻抓出来。
* `_trayclick_test.py` 27 项（补了「重新注册失败路径不许 sleep」的纯逻辑护栏）。
* 顺手修了两个**测试自己**的假 PASS：一是 `_frostlive.py` 没看「锁定位置」这个开关，
  长条锁定后本来就是故意点击穿透的，右键该落到任务栏上（现在明确 SKIP 并说明原因）；
  二是上面那个背景图判据第一版阈值贴着背景噪声，不铺也 PASS。

## 附：v1.0.15 的内容（上一版）

点出来的另外两件事 —— 常驻任务栏之后右键失灵（注册信息被改窄）、双击不出卡片
（双击的两次「抬起」各切换一次面板），以及一次大改：**新增「长条外观」窗口**，
排数 / 字号 / 大小 / 配色 / 背景图 / 细节全部交给你自己决定，顶部带实时预览。
详见仓库里的 `NOTES.v1015.md`。
