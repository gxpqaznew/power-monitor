这一版修的是两个用起来最别扭的地方：菜单勾选后消失、以及记录一更新就没了。

## 零、先补一条：上一版的安装程序会把账本删掉

v1.0.6 把账本挪到了固定位置 `%LOCALAPPDATA%\PowerMonitor\`，但安装脚本里还留着
一段 v1.0.0 时代的「旧便携目录清理」——它以为那个目录是老旧版本的残留，收尾时
`Delete "${LEGACY_DIR}\state.json"`。于是**每装一次，历史就归零一次**：刚攒下的
3605 Wh 账本被安装程序当场删掉，程序一启动又从头开始记，现象和这一版想修的
「更新就没记录」一模一样，所以第一眼根本认不出来是安装程序干的。

现在：安装/卸载**都不再碰**用户数据目录。另外补了两道保险——

- 每次启动会把账本另存一份 `state.json.prev`（覆盖式，只留上一代）。`state.json`
  不管是被谁弄丢、弄坏，下次启动会自动从这个副本接手，记录还在。
- `_lint.py` 加了一条静态检查：安装脚本里只要出现针对用户数据目录的
  `Delete` / `RMDir` 就报错。这类「安装程序多手删数据」的坑不能再踩第二次。

## 一、托盘右键菜单勾完不再关闭

以前勾一个「长条字号」，整个菜单就关掉了，想再勾一个「长条大小」得重新右键点一遍，
连着调三项就得点三遍。

现在四个长条外观子菜单（质感 / 字号 / 大小 / 显示内容）**勾完菜单原地保留**，
可以连续勾完所有想调的项，最后按 `Esc` 或点菜单外面空白处关掉。

顶层那三个勾选式开关也一样：**开机自启动 / 常驻任务栏 / 任务栏长条读数**，点完
菜单留在原地，可以接着勾下一个。普通动作命令（打开面板、电价设置、重置、退出…）
仍然照常点一下就关 —— 那些不需要连着点。

实现上没有魔法：Win32 的 `TrackPopupMenu` 本来就是「选中一项即关闭」，
改不了它。所以是**在同一位置把同一级菜单重新弹一次**——菜单重建（勾选状态是新的）、
坐标固定（菜单不会点一下跳一下），手感上就是「没关」。连续操作上限 40 轮，
够用且不会卡死。

## 二、电量和电费记录不再丢失

以前每次更新版本、或者关掉程序再打开，统计就从头开始，历史记录全没了。
根子在于记录文件跟着 exe 走：程序装在哪儿、`state.json` 就写在哪儿。
更新版本时安装目录被覆盖，老记录跟着一起没了。

现在做了三件事：

- **记录存到固定位置**：`%LOCALAPPDATA%\PowerMonitor\`，跟程序装在哪、装几次都无关。
  升级、卸载重装、换便携版，记录都在。
- **按天记账**：每天一行「电量 / 电费 / 运行秒数」，最多保留 400 天。
  面板上的功率卡片和长条都新增了**本月**和**累计**两组数字。
- **退出必存**：程序正常退出、注销、关机（`WM_QUERYENDSESSION` / `WM_ENDSESSION`）
  都会先落盘。写文件走「临时文件 + 原子替换」，中途断电也不会写出半个坏的 JSON。
  顺带把自动保存间隔从 60 秒缩短到 15 秒，掉电最多丢 15 秒。

**老用户的记录会自动搬过来**：第一次启动时会在几个常见位置找历史记录，
按「累计电量最大」挑一份（累计值是单调递增的，最大的一定是记录最全的那份），
搬进新位置；没被选中的文件也不会删，留一份 `.bak` 在旁边。

## 三、面板显示

详情面板功率卡片右侧，从原来只显示「今日累计」，改成**今日 / 本月 / 累计**
三行，每行同时给电量和电费。长条的可选字段也新增「本月电量」「累计电量」
「累计电费」三项（默认不勾，长条不会因此变宽）。

## 四、自检

`能耗统计.exe --self-test` 从 32 项扩到 **49 项**，新增了守着上面这些行为的检查：
记录目录是否落在固定位置、老版「今日」数据能否折算进按天账本、本月合计、
累计电费回填、按天账本存取往返、历史上限、保存间隔、累计字段进长条、
子菜单重建后锚点是否还对、顶层勾选式开关是否登记成「点完不关」，以及
**「state.json 被清掉时能不能自动接手上一代副本」**——最后这条就是冲着上面
第零节那个事故写的。
