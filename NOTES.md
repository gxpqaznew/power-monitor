任务栏长条（读数）的外观和显示内容，现在全都可以自己调。

托盘右键菜单新增四个二级子菜单，改完立刻生效，不用重启、不用重装：

- **长条质感** 6 档：跟随任务栏（默认）/ 玻璃 / 线框 / 深色卡片 / 浅色卡片 / 强调色
- **长条字号** 5 档：小 / 标准 / 大 / 特大 / 巨大
- **长条大小** 3 档：纤细 / 标准 / 宽大
- **长条显示内容** 12 个字段任意勾选：当前功率、处理器 / 显卡 / 其他功耗、
  本次电费 / 电量、今日电量 / 电费、平均 / 峰值功率、开机时长、当前时段

几条约定：默认档（标准大小 + 标准字号）算出来的胶囊高度与旧版**完全一致**，
老用户升级后长条不会变个高度；字号调大时胶囊高度会跟着抬，字不会被上下切掉；
菜单里只剩最后一项时会变灰点不动（长条总得显示点什么）；没有能读功耗的独显时
「显卡功率」那一段直接不显示，不会显示一个恒为 0 的假数字。

## 同时修掉两个渲染缺陷

- **半透明质感出现规则横条纹**：`compose_shape_alpha` 里 `inside()` 是闭包，
  捕获外层的 `k`；而「边缘」和「阴影」两个分支也往同一个 `k` 里写值。于是每行
  一旦处理过一个边缘像素，该行之后所有内部像素就都按那个边缘像素的系数去乘 RGB。
  这个坑老代码里就埋着，但长条一直是不透明的 `alpha=255`（走不到那条分支）所以没
  暴露，加了半透明质感才炸出来。
- **线框主题文字发紫**：原来用一个醒目的品红当「抠色哨兵色」，而文字是抗锯齿画的，
  字边那圈像素是「文字色 × 底色」的混合色，既不等于品红也抠不掉，于是整行字镶
  一圈紫边。改成用**背后的真实任务栏色**当底色，字边的混色恰好就是「文字直接画在
  任务栏上」应有的颜色，原样保留即正确。

## 另外

托盘菜单「关于」里显示的版本号之前一直停在 `1.0.0`（发布版本号的真源在 NSIS 安装
脚本里，两边没同步），现在发布脚本会把 `powermon/__init__.py` 的 `__version__`
一起写，不再对不上。

## 自检

`能耗统计.exe --self-test` 现在有 32 项，新增了「六种质感都能离屏画出来且内部无
空洞」「默认档高度比例仍是 0.78」「最后一项显示内容不允许取消」「非法配置能夹回
合法档」以及守着上面那个横条纹 bug 的「半透明合成逐行一致」。
