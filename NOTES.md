这一版修的是上一版之后被点出来的四件事：**累计电费居然比本次电费低**、**统计还是黑箱
（只能看汇总、不能挑具体某天 / 某次开机）**、**长条位置固定且折行后两排不对齐**、
以及**长条两端那两列小圆点太丑**（后者带来了一次设计上的回头路，见第三节）。

## 一、累计电费比本次电费还低（真 bug）

现象很直白：面板上「本次电费 ¥0.82」，下面「累计电费 ¥0.61」。

成因是**累计电费被当成一个独立累加器**在跑：老版本只按「当天一个桶」存，回填历史时
不知道缺的那一截是按什么电价算的，于是凭空少算；而「本次」「今日」是另一条路径算出来的，
两条路径的口径从一开始就不一样。

这一版把它改成**只有一个真源**：

- 账本里**只记电量，不记电费**（`ledger_version: 2`）。峰段电量 / 谷段电量分开存，
  因为同一度电在不同时段价格差 3 倍，只存总量就再也算不回来了。
- **所有电费都是展示时按电量现算的**：`累计 = Σ每日电费 + 还没有每日明细那部分的电费`；
  本次同理。账本里存的就是电量这一件事，不存在两个数对不上。
- 老账本（v1）在加载时自动折算成「峰段 / 谷段电量」，按当时的时段比例分摊 ——
  不丢历史，也不用你去删 `state.json`。
- 加了一条**单调性保护**：累计电费不允许低于本次 / 今日（电量是单调涨的、单价是同一套），
  万一还有哪个边角路径算歪了，也不会再出现「累计 < 本次」这种一眼假。

`_ledger_test.py` 里专门有一组断言守着它：跨天折算、峰谷拆分、存取往返、坏值容错、
上限裁剪、丢文件后从 `state.json.prev` 接手、退出强制落盘。

## 二、用量统计：能自己挑一条看了

上一版有了按天 / 按月 / 按年 / 按每次开机 / 按时段五页，但只能看「一览」——
想知道「上周三到底用了多少」「第 37 次开机那阵在干什么」还是得自己数行。

现在窗口底部多了一排 **「选一条看明细」**：

```
 选一天  [ 2026-09-14 周一          ▾ ]  ◀  ▶   ┌──────────────────────────────┐
                                                │ 2026-09-14 周一              │
                                                │ 电量 1.24 kWh   电费 ¥0.61    │
                                                │ 运行 6 小时 12 分  开机 2 次   │
                                                └──────────────────────────────┘
```

- 下拉框里的选项**跟着当前标签页变**：在「按天」页选的是日期，在「按每次开机」页选的
  就是「第 N 次开机 · 09-14 15:00」，在「按时段」页选的是「08 时」。「选一天」这个标签
  也会跟着改成「选一次开机」「选一个时段」。
- 下拉框选中哪条，**上面表格就自动滚到那一行并选中**，两边联动，不用自己找。
- `◀ ▶` 是前后翻，键盘上下键也行。
- 明细框是只读的（`ES_READONLY | ES_MULTILINE`），一次把那条记录的全部数字摊开：
  电量 / 电费 / 运行时长 / 开机次数，按页不同给不同的字段组合。
- 翻到空账本时不会静默留个白框，明说「还没有可看的记录 —— 程序跑一会儿就会有了」。

顺带把窗口底部那行提示重写了：原来是一长串并列的说明，窄窗口下会折行溢出、压到下一行上。
现在是**三组按重要性排序**，宽度不够就从最不重要的那组开始丢（「已记录 N 天 · M 次开机」
最先丢，「点表格任意一行 = 下面就是那一条的全部数字」永远留着）。

## 三、长条：能拖了，而且是一条干净读数条

**位置**：上一版给长条做了拖动，但做法是**在胶囊两端各画一列小圆点当把手**（两个独立的
分层子窗口，各自垫一层 `alpha = 2/255` 骗命中）。用户看了一眼，原话是「不要那左右六个点，
太丑了，你参考一下别人 GitHub 上做到 codex meter 这种审美」——

这一版就把整个把手机制**删掉**，改成**整块胶囊自己就是拖动面**：

- 平时它只是一条干净的读数条，**没有任何常驻装饰**；
- 鼠标指上去时描边转成强调蓝、加粗一档（`_apply_hover` 只动 `border` / `border_w` /
  `bg` 三个键），移开就收 —— 这是「这里可以拖」的全部提示；
- 按住横拖即走、双击复位、拖出屏幕自动夹住。

能做这么干净，是因为**分层窗口的命中测试本来就是按像素 alpha 走的**：胶囊内部
alpha=255，鼠标点得到；圆角外面（以及描边外侧那几个抗锯齿像素）alpha=0，鼠标天然穿过去。
所以「本体可拖 + 圆角外不挡任务栏」不用任何技巧。上一版之所以要另开两个窗口，是因为
它把长条整块设成了 `WS_EX_TRANSPARENT`（纯显示、彻底不碰鼠标），于是只能在外面挂两块
窗口来收拖动消息 —— 而那两块又必须是**任务栏的兄弟窗口**（做子窗口会被父窗口的
`WS_EX_TRANSPARENT` 连着穿透），还要靠 `alpha = 2/255` 才能被 `WindowFromPoint` 命中。
一整套都是为「穿透」付的代价；不穿透之后，这套东西就全是多余的：

> 反过来，**去掉穿透是有代价的**：长条现在会吃掉自己那块区域的鼠标，任务栏右键菜单
> 点不出来。所以长条自己接 `WM_RBUTTONUP`、回调 `menu_callback` 把托盘那套菜单弹回来；
> 真想让它彻底穿透的用户，就在「长条位置」里勾上**锁定位置** ——
> `set_interactive()` 用 `SetWindowLongPtrW(GWL_EXSTYLE)` 加回 `WS_EX_TRANSPARENT`
> 再补一个 `SWP_FRAMECHANGED` 让系统重算非客户区。

拖动偏移存的是**设计基准像素**（跟 DPI / 任务栏大小一起缩放），换显示器或者改任务栏
高度之后位置依然合理。`_dragtest.py` 是一整套真机拖动回归（26 项，跑在临时目录里、
不碰你真实的 `config.json`），其中三项专门守着「**再也不许出现把手窗口**」：任务栏子树里
枚举类名，出现 `PowerMonitorStripGrip` 就判 FAIL。

> 删把手的顺手收益：旧实现里「把手不能做子窗口」「位图必须垫非 0 alpha」这两条坑
> 连同它们对应的静态守卫一起作废了。`_lint.py` 里的守卫换成三条新的：
> ① 把手机制（`_GRIP*` / `PowerMonitorStripGrip`）一旦长回来就报错；
> ② `_EX_BASE` 里不许混进 `WS_EX_TRANSPARENT`（带了长条就永远收不到鼠标、拖不动，
> 而且失效得很安静 —— `_interactive` 照样是 True）；③ 建窗口那行必须按
> `self._interactive` 算扩展样式。

**对齐**：以前折行之后是「每行各排各的」—— 这一行的标签宽度只按这一行的字段算，
于是第二行的标签起点和第一行错开、分隔线也各在各的位置，看着就是乱的。

现在先把字段切成 `行 × 列`，**列宽取该列所有格子的最大值**，于是标签起点 / 数值起点 /
分隔线 x 只由「第几列」决定，两行天然重合。三层都有断言：

- 逻辑层：规划结果返回列结构（`_split` + `_grid`），列宽必须等于该列最大值；
- 绘制层：`_stripfields_test.py` 里挂了个 `DrawSpy` 记录每次 `DrawText` 的坐标，
  按 y 分组后断言两行的标签 x / 数值 x / 分隔线 x 逐列相等（6/8/10/12/15 字段都跑）；
- 像素层：`_striplive.py` 真机建出长条 + 抓屏，从位图里数分隔线的真实 x ——
  实测两行落在同一批 x 上：`[114, 204, 294, 295, 384, 385, 488, 618]`。

> 这里也顺手修掉一个**假 PASS**：起初颜色判据写反了（`COLORREF` 低字节是 R，
> 而 DIB 里是 BGRA），结果「一条分隔线都没找到」却依然报通过。
> 现在加了护栏 —— 没找到分隔线直接判 FAIL。

## 四、自检

`能耗统计.exe --self-test` 从 57 项扩到 **67 项**。新增的都在守这类**「逻辑全对、
就是画不出来」**的故障：长条网格列宽、位置子菜单入口数、「锁定位置 / 位置复位不会被
当成档位命令」、拖动偏移的合法性夹取，以及「悬停配色只改描边那几个键」。

回归清单也补了几个诊断脚本：`_hoverprobe.py`（按真实落点离屏渲染普通态 / 悬停态两张图，
像素级断言「悬停只改了描边那一圈」，实测差异 4.15%）、`_aligndbg.py`（打印分隔线的真实
像素颜色，专门用来查字节序问题）。`_gripprobe.py` 留作历史诊断 —— 把手已废，但那份
「子窗口 vs 任务栏兄弟窗口的命中差异」实测记录还值得当 Win32 命中测试的教材。

跑过的全套：`_ledger_test.py`(38) · `_stats_test.py`(33) · `_stripfields_test.py`(54)
· `_dragtest.py`(26) · `_statsshot.py`(16) · `_shouldhide_test.py`(8) ·
`_stripwidth_test.py`(5) · `_hoverprobe.py` · `_striplive.py` · `_roundtest.py` ·
`_panelstates.py` · `_lint.py` · `self_test`(67) —— 全绿。
