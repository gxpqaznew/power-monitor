这一版把「右键菜单」这条链路**再加固一层**：菜单弹出时抢前台的动作原来夹在消息处理
中间做，会被「刚刚那次点击」带得焦点一抖，菜单可能刚出现就被自己关掉。现在把它推到最后
做，并给刚弹出的几百毫秒加了落座宽限 —— 同时上闸：**那几百毫秒里只要鼠标键是按着的
（＝用户真的去点了别处），宽限立刻让位**，不会被抢回来。

顺带把上一版发现的另一个坑补上（背景图不透明度写成小数会静默失效），以及一批**测试
自身的假 PASS**（这才是真正把前两个版本的 bug 一直放过去的原因）。

## 一、菜单弹出时的抢前台，改成「推后做」+ 落座宽限

自绘菜单要抢到前台，才能收 `WM_KILLFOCUS` 来判断「用户点了别处」。问题是抢的**时机**：

`show()` 是在调用方的窗口过程里**同步**跑的 —— 托盘那条路上，explorer 是用
`SendMessage` 把点击转发过来的，也就是说**此刻 explorer 还没处理完这次点击**。
我们抢到前台、返回，explorer 接着处理，把前台还给任务栏 → 我们刚抢到的立刻被顶掉 →
菜单收到 `WM_KILLFOCUS` → 判定「点别处了」→ 关掉。

用户看到的就是**「右键点了没反应」**（菜单出现过，只是存在了几十毫秒）。

改三处：

1. **抢前台推后一条消息再做**（`PostMessageW` 自己）。这样就轮到最后抢，站得住。
2. 刚弹出后的 `0.35` 秒算**落座期**：这段里收到的 `WM_KILLFOCUS` 不当「点别处」，
   先重抢前台（最多 3 次），抢不回来才真关。打开菜单的那次点击本身会带得焦点晃一下，
   那不是用户的意思。
3. 🔴 **宽限让位于「用户真的按了鼠标」**：判据是**此刻有没有鼠标键按着**。
   用户点别处时，激活变更由那一下按下触发，`WM_KILLFOCUS` 是在「按下」那条消息里
   同步送来的，读 `GetAsyncKeyState` 一定是按下；而 explorer 处理完点击、把前台还给
   任务栏那一下早松手了。少了这条，宽限期内点别处菜单会**把前台抢回来** ——
   等于把毛病从「弹不出来」换成了「关不掉」。

真机实测（`_stripmenuprobe.py`，真 `SendInput` 右键长条正中，8 轮）：
菜单 **8/8** 在 0.49~0.55 s 出现并稳定存在；日志里「落座期收到 KILLFOCUS」**0 次** ——
推后抢之后连抖动都没有了。

`WM_KILLFOCUS` 的三种来源在 `_menulive.py` 里各用**真信号**验一遍（构成一个 2×2）：

| | 有鼠标键按着 | 没键按下 |
|---|---|---|
| **落座期内** | 立刻关，0 次重抢（E2） | 重抢前台，菜单留着（E1） |
| **过了落座期** | —— | 立刻关（E3） |

E2 的按键是 `SendInput` 真按下的（`GetAsyncKeyState` 读回 `True`），不是 mock 判据函数；
E1/E2/E3 用**同一个**刺激（清焦点 + 让出前台），只有时间 / 按键两个变量在变。
反向验证过**断言是有效的**：把「有无按键」这条判据拿掉（＝回到 v1.0.16 的盲宽限）再跑，
E2 立刻 FAIL，读数 `closed=False tries=2` —— 菜单果然把前台抢回来了。

> ⚠️ 说清楚证据强度：这个抖动我**没能稳定复现**（一次 6 轮小样本里出现 2 次「刚出现
> 30 ms 就消失」，随后的 20+20 轮和 4 次走查都是干净的）。所以这一条是**预防性加固**，
> 不是「已确证的缺陷修复」。同时反向验证了**没改坏核心行为**：弹出后点桌面
> **4/4 关掉**、弹出后不动 **3/3 保持**。

> 顺带记一个探针自身的坑：**只发 `SetForegroundWindow` 不足以造出「切走程序」**——
> 从自己进程调它，系统不保证把本线程的焦点也清掉（实测同一套代码：0.08 s 时送了
> `KILLFOCUS`，0.5 s 时压根没送）。于是「菜单没关」到底是产品不关还是探针没送到刺激，
> 根本说不清 —— E3 一度因此假 FAIL。现在统一加一手 `SetFocus(NULL)`（同线程内直接
> 送 `WM_KILLFOCUS`），并把「前台确实让出去了」单独列一条前置断言。

## 二、背景图不透明度写成小数会静默不生效（真 bug）

「不透明度」量纲是 **0~100**，配置文件是明文，手改写成 `0.85` 很自然 —— 而 `int(0.85)`
就是 **0**，0 的语义是「不画背景图」。表现：设了图、滑块也拉上去了、文件明明在那儿，
**就是什么都不显示**，而且一声不响（图片读不到 / 路径不对 / 解码失败，全都是安静跳过）。

现在小数按分数认：`0.85` → 85%、`1.0` → 100%；整数照旧（写 `1` 就是 1%）。

## 三、一批「测试自身的假 PASS」（这才是前两版 bug 被放过去的原因）

1. **断言只问「最终能不能成」**：真机走查里那一项写的是「重新注册之后**最终**能不能
   弹出菜单」—— 带 4 秒阻塞的老版本照样能弹（只是慢），于是走查 **7/8 全绿**。
   改成量墙钟（≤ 2.5 s）之后老版本立刻 5.55 s FAIL。
   *写完断言就问自己：把修复回滚掉，它会红吗？不会红的断言只是日志。*
2. **探针素材自己写歪了**：给「背景图铺上去了没有」写判据时，探针 PNG 生成函数多写了一个
   filter 字节，导致每行像素错位 1~120 字节 —— PNG 能打开、确实是彩色的、肉眼看不出来，
   但整张图是花的。于是那条判据一直在验一张随机图。现在加了「探针图每行相同、B 通道
   恒 255」的自检。
3. **阈值贴着背景噪声**：判据写成「探针色 ≥ 200 px」，而窗口圆角外透出的桌面本来就有
   170~235 个同色像素 → **根本没铺上去也 PASS**。改成按实测基线校准
   （`max(600, 基线×3)`），并且**同时**要两种互斥色（洋红 + 青）。
   实测信号 2954 / 4049 px vs 基线 0 / 169 px。
4. **别盯具体 hwnd 判「菜单关掉没有」**：菜单窗口可能在两帧之间被替换，盯 hwnd 会偶发
   假 FAIL。改成按「屏幕上还剩几个菜单窗口」判。
5. **长条「锁定位置」时会故意点击穿透**（`WS_EX_TRANSPARENT`）：锁定时在长条上右键
   本来就该落到任务栏上。`_frostlive.py` 以前不看这个开关 → 用户把长条锁了就假 FAIL。
   现在明确 SKIP 并说明原因。
6. **两帧永远别用 `==` 比**：圆角外透出的桌面 + 毛玻璃噪声，同一个档位重抓两次也会差
   几百像素。要比差异量级 —— 同档 711 px（0.1%）vs 换档 19228 px（整块预览条）。

## 四、新增的验证设施

* `_appearancelive.py`（**22 项**）：真机真显示 + 真 `SendInput` 点击 + **从屏幕**抓图。
  离屏测试用合成消息，验不了「显示出来之后长什么样、真鼠标点不点得动」。
* `_stripmenuprobe.py`（**8 项**）：真机右键长条，量菜单「出现时刻 / 存活时长」。
  为什么不用托盘图标：**dev 版是 `pythonw.exe`，图标落在 `^` 溢出里，
  `Shell_NotifyIconGetRect` 照样报得出坐标，但那个位置点不到东西** —— 踩过，
  表现为探针 10/10「从未出现」，白查半天。长条是任务栏的子窗口，一定点得到。
* `_trayaddprobe.py`：量「对已存在图标重复 `NIM_ADD` 的代价」（结论：立刻失败、0.6 ms、
  连续 3 次共 4.00 s —— 这就是老版本 4 秒阻塞的来源）。
* `_menuflakeprobe.py`：菜单出现/早夭的时间线采样。
* `_escprobe.py`：ESC 关菜单的对照实验。
* `_traylive.py` 重写（**11 项**）：加上「等程序进稳态」—— 冷启动前十几秒点它没用
  （还在采第一帧、建长条、暖毛玻璃），实测刚启动 12 s 时第一下右键会失灵；
  以及把判据改成量时间。
* `_menulive.py`（**20 → 29 项**）：`WM_KILLFOCUS` 那一段重写成上面那张 2×2，
  另外补「`GetAsyncKeyState` 声明了 `restype=c_short`」（按 `c_int` 读会拿到 EAX 的
  脏高位，「按下」判据会随机）和「没按键时读出来是没按住」两条。

## 附：v1.0.16 / v1.0.15 的内容

* **v1.0.16**：修「重新注册那几秒托盘是卡住的」—— `TaskbarCreated` 是在窗口过程里
  **同步**处理的，而失败重试会 `sleep(2.0)`×2 = **4 秒**，这 4 秒托盘收不到任何消息。
  实测「从收到重注册到右键弹出菜单」**5.55 s → 0.79 s**。修法：不阻塞 + 失败退到
  `NIM_MODIFY` 重新登记（实测对已存在的图标重复 `NIM_ADD` 必失败，而 `NIM_MODIFY`
  成功 —— 图标本来就在，不用等）。
* **v1.0.15**：修「常驻任务栏之后右键永久失灵」（`NOTIFYICONDATA.uFlags` 被日常刷新
  越改越窄 → 重注册时回调根本没登记）+「双击不出卡片」（双击是
  `down→up→DBLCLK→up`，两次 `up` 各 toggle 一次 = 开了又关）+ 新增「长条外观」窗口
  （排数 / 无级字号 / 大小 / **11 套配色** + 三自定义色 / 背景图三种填充 + 不透明度 /
  两个细节开关，顶部实时预览）。详见 `NOTES.v1015.md`。
