这一版修的是被点出来的三件事：**选了「常驻任务栏（^ 左侧）」之后右键弹不出菜单**、
**双击托盘图标不出卡片**，以及**长条能调的地方太少**（以前只能选字段和六档字号）。
第三件是一次大改：新增「长条外观」窗口，排数 / 字号 / 大小 / 配色 / 背景图 / 细节
全部交给你自己决定。

## 一、常驻任务栏之后右键没反应（真 bug）

现象：把图标设成「常驻任务栏」之后，右键弹不出菜单、双击也不出卡片 —— 图标本身
看着完全正常，鼠标悬停的提示文字也还在。

成因是 `Shell_NotifyIcon` 的 `uFlags`：它决定「这一次注册 / 修改管哪些字段」。程序每秒
刷新一次图标上的数字和提示文字，每次都把 flags 越改越窄，稳态只剩「提示文字」这一项。
平时看不出来（改单个字段本来就够用），但一旦触发**重新注册** —— 重启资源管理器，或者
点「常驻任务栏」—— 就带着「只有提示文字」的注册信息去登记，回调消息没登记进去，
于是右键 / 双击 / 单击**全都没有回调**。

修法：每次注册前把注册信息复位成完整的一份；日常刷新只在**副本**上改，不动原件。

## 二、双击托盘图标不出卡片（真 bug）

Windows 的双击消息序列是「按下 → 抬起 → **双击** → 抬起」，两次「抬起」各把面板切换
一次（弹出来又立刻收回去），看起来就是「双击没反应」。

现在第一下抬起照旧（单击依然零延迟），双击和随之而来的第二下抬起改成「确保显示」——
幂等，不会把刚弹出来的卡片又收回去。万一只收到两次快速「抬起」而没有双击消息，
还会用系统的双击间隔兜底判一次。

## 三、长条能自己调的地方（新增「长条外观」窗口）

托盘菜单 →「长条外观…」，一个窗口里全部搞定：

- **排数**：自动 / 一排 / 两排 / 三排。字段多了不用再靠自动折行碰运气。
- **字号**：滑块无级调（0.60×~1.60×）+ 六档快捷取值。
- **大小**：纤细 / 标准 / 宽大。
- **配色**：11 套整组配色（跟随质感，以及石墨、午夜、松林、落日、酒红、墨玉、宣纸、
  樱花、薄荷、晴空），每套都按对比度挑过 —— 长条是贴在任务栏上的一条细带，
  颜色一淡就整条糊成一片，所以数值色一律保证 4.5:1 以上。不想用整套的，
  可以单独自定义标签色 / 数值色 / 底色。
- **背景图**：选一张图铺在毛玻璃之上（填满 / 适应 / 平铺，不透明度可调），
  按胶囊的圆角裁剪，所以不会像贴歪的便利贴。
- **细节**：显示标签、显示分隔线（关掉标签能多塞下四成字段）。
- 窗口顶部有**实时预览**：预览走的是长条自己的排版代码，所以看到的就是任务栏上那条的
  真实样子，不是示意图。

外观改动立刻生效；不喜欢的点「恢复默认外观」一次回出厂 —— 只清外观，不动你勾了哪些
字段、也不动长条拖到哪儿。

## 四、顺手修掉的三个坑（都是这版新代码里的）

1. **滑块拖不动**：按下时按 4 个值解包一个 3 个值的结构，异常被消息循环吞掉，
   表现成「拖了没反应」且毫无报错。
2. **窗口底部的「恢复默认 / 完成」看不见**：有一段函数的返回值是「绝对坐标」，
   调用方当成「高度」又加了一次，内容凭空多出 600 多像素，把底部按钮顶到窗口外面。
3. **设了背景图之后，「排列」整段和「字号」标题消失**：还原裁剪区用错了 API ——
   `SelectClipRgn` 返回的是「区域类型码」而不是旧的区域句柄，拿着类型码当句柄传回去
   会把 DC 的裁剪区搞坏，后面画的东西被裁掉一大块。

## 五、回归

自检从 73 项扩到 **90 项**（补了外观目录、配色对比度、排数真的作用到排版、
背景图几何、"新模块有没有打进包" 等）；新增三个回归脚本：
`_appearancetest.py`(49)、`_appearanceshot.py`(49，真建窗口画一帧 + 按登记区域点一遍)、
`_appearancepng.py`(出对照图)。
